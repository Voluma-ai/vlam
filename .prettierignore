dist
dist-demo
.wrangler
assets
coverage
test-results
playwright-report
blob-report
package-lock.json
.tmp

# Prose is hand-wrapped to fit a terminal; Prettier would reflow it.
*.md

# The demo pages' inline CSS reads worse formatted: Prettier splits shorthands
# like `font: 13px/1.5 system-ui, sans-serif` across three lines.
*.html
