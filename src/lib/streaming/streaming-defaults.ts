/** Default splats staged per classic streaming mutation tick. */
export const DEFAULT_CLASSIC_SPLATS_PER_SWAP = 32_000;

/** Default pool writes delivered per RAD page-table worker plan. */
export const DEFAULT_PAGE_TABLE_WRITES_PER_PLAN = 16_000;
